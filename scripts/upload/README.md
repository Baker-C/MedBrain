# Upload script

Placeholder. The one-time local script that seeds `DocumentCorpus/` PDFs into the private
Supabase Storage bucket (Build-Spec §4) will live here. Populates storage only; never
writes the registry.
